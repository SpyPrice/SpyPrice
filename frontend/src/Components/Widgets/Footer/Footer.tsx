import styles from './Footer.module.scss'

export const Footer = () => {
	return (
		<footer className={`${styles.footer}`}>
			© 2026 Price Tracker. Все права защищены.
		</footer>
	)
}

export default Footer
